//rotate array by k 


vector<int> rotateArray(vector<int>arr, int k) {
    int index=0,n=arr.size();
    vector<int> ans;
    for(int i=k;i<n;i++){
        ans.push_back(arr[i]);
    }
    for(int i=0;i<k;i++){
        ans.push_back(arr[i]);
    }
    return ans;
}
